[defaults]
inventory = inventory/hosts.yml
remote_user = ubuntu
private_key_file = ../terraform/apexpos-key.pem
host_key_checking = False
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
