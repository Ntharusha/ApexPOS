apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: apexpos
data:
  PORT: "5000"
  NODE_ENV: "production"
  ALLOWED_ORIGINS: "https://apexpos.vercel.app" # Default Vercel subdomain
